#! /usr/bin/env python
# encoding: utf-8

def configure(conf):
    conf.sub_config('gxwmm');

def build(bld):
    bld.add_subdirs('gxwmm')
    bld.add_group()
    prog = bld(features = 'cxx cprogram',
               source = 'gxw_demo.cc',
               target = 'gxw_demo',
               use = ['GTKMM','gxwmm','gxw'],
               includes = ['.','../libgxw'],
               install_path = None,
               )
